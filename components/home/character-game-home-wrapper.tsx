/**
 * @file character-game-home-wrapper.tsx
 * @description CharacterGameHome 래퍼 컴포넌트
 * 
 * 3D 뷰어 기능이 제거되었습니다.
 * 이 컴포넌트는 더 이상 아무것도 렌더링하지 않습니다.
 */

'use client';

export function CharacterGameHomeWrapper() {
  // 3D 뷰어 기능 제거됨 - 아무것도 렌더링하지 않음
  return null;
}

