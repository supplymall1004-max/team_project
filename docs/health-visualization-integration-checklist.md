# 건강 시각화 기능 통합 체크리스트

## 📋 프로젝트 통합 전 검증

이 문서는 건강 시각화 기능을 실제 프로젝트에 통합하기 전 확인해야 할 사항들을 정리합니다.

---

## ✅ Phase 1: 기본 설정 및 의존성 확인

### 1.1 필수 패키지 설치 확인

```bash
# package.json 확인
□ @radix-ui/react-progress: 프로그레스 바 컴포넌트
□ lucide-react: 아이콘 라이브러리 (이미 설치됨)
□ recharts (선택): 고급 차트 기능 원할 경우
```

**설치 명령어:**
```bash
pnpm add @radix-ui/react-progress
```

### 1.2 타입 정의 확인

```typescript
□ types/health-visualization.ts 생성 완료
□ 기존 types/health.ts와 호환성 확인
□ types/recipe.ts의 Ingredient 타입 import 가능 확인
```

**검증 명령어:**
```bash
pnpm tsc --noEmit
```

### 1.3 UI 컴포넌트 확인

```bash
□ components/ui/card.tsx ✅
□ components/ui/badge.tsx ✅
□ components/ui/button.tsx ✅
□ components/ui/tabs.tsx ✅
□ components/ui/progress.tsx ✅ (신규 추가)
```

---

## ✅ Phase 2: 컴포넌트 통합 검증

### 2.1 건강 시각화 컴포넌트 파일 확인

```bash
□ components/health/visualization/HealthDashboard.tsx
□ components/health/visualization/HealthMetricsCard.tsx
□ components/health/visualization/NutritionBalanceChart.tsx
□ components/health/visualization/MealImpactPredictor.tsx
□ components/health/visualization/HealthInsightsCard.tsx
□ components/health/visualization/DiseaseRiskGauge.tsx
```

### 2.2 컴포넌트 import 테스트

```typescript
// 테스트 파일: app/test/health-visualization-test/page.tsx
import { HealthDashboard } from '@/components/health/visualization/HealthDashboard';
import { HealthMetricsCard } from '@/components/health/visualization/HealthMetricsCard';
// ... 모든 컴포넌트 import

□ import 오류 없음
□ 타입 오류 없음
□ 빌드 성공
```

### 2.3 컴포넌트 렌더링 테스트

```bash
# 개발 서버 실행
pnpm dev

# 브라우저에서 확인
http://localhost:3000/test/health-visualization-test

□ HealthDashboard 렌더링 확인
□ 스켈레톤 로딩 상태 확인
□ 에러 상태 UI 확인
□ 반응형 디자인 확인 (모바일/데스크톱)
```

---

## ✅ Phase 3: API 엔드포인트 검증

### 3.1 API 파일 확인

```bash
□ app/api/health/metrics/route.ts
□ app/api/health/meal-impact/route.ts
```

### 3.2 API 기능 테스트

```bash
# 건강 메트릭스 API 테스트
curl -X GET http://localhost:3000/api/health/metrics \
  -H "Authorization: Bearer YOUR_TOKEN"

□ 200 응답 확인
□ HealthMetrics 타입 구조 일치 확인
□ 계산 로직 정확성 검증

# 식단 효과 예측 API 테스트
curl -X POST http://localhost:3000/api/health/meal-impact \
  -H "Content-Type: application/json" \
  -d '{
    "mealType": "breakfast",
    "mealData": {...},
    "currentHealth": {...}
  }'

□ 200 응답 확인
□ MealImpactPrediction 타입 구조 일치 확인
□ 예측 로직 정확성 검증
```

### 3.3 인증 및 권한 확인

```bash
□ Clerk 인증 통합 확인
□ Supabase RLS 정책 확인
□ 사용자별 데이터 격리 확인
```

---

## ✅ Phase 4: 데이터베이스 스키마 확인

### 4.1 기존 테이블 확인

```sql
-- users 테이블 필드 확인
□ clerk_id: TEXT
□ age: INTEGER
□ gender: TEXT
□ height_cm: INTEGER
□ weight_kg: NUMERIC
□ activity_level: TEXT
□ diseases: TEXT[] 또는 JSONB
□ allergies: TEXT[] 또는 JSONB
```

### 4.2 추가 테이블 필요 여부 확인 (선택)

```sql
-- 건강 메트릭스 캐시 테이블 (성능 최적화용)
CREATE TABLE IF NOT EXISTS user_health_metrics_cache (
  user_id UUID PRIMARY KEY REFERENCES users(id),
  metrics JSONB NOT NULL,
  last_calculated TIMESTAMP DEFAULT NOW(),
  calculation_version INTEGER DEFAULT 1
);

□ 테이블 생성 여부 결정
□ 캐싱 전략 수립
```

---

## ✅ Phase 5: 페이지 통합 및 라우팅

### 5.1 식단 상세 페이지 디렉토리 구조

```bash
□ app/diet/breakfast/[date]/page.tsx
□ app/diet/lunch/[date]/page.tsx
□ app/diet/dinner/[date]/page.tsx
```

### 5.2 페이지 구현 상태 확인

```typescript
// 각 페이지에서 확인할 사항
□ 날짜 파라미터 파싱 정상 작동
□ 식단 데이터 로딩 성공
□ 건강 시각화 컴포넌트 통합
□ 에러 핸들링 구현
□ 로딩 상태 UI 구현
```

### 5.3 라우팅 테스트

```bash
# URL 접근 테스트
□ /diet/breakfast/2024-12-10
□ /diet/lunch/2024-12-10
□ /diet/dinner/2024-12-10

# 네비게이션 테스트
□ DietCard에서 상세 페이지로 이동
□ 뒤로가기 버튼 작동
□ 날짜 변경 시 재로딩
```

---

## ✅ Phase 6: 성능 최적화 검증

### 6.1 렌더링 성능

```bash
# React DevTools Profiler 사용
□ 초기 렌더링 시간 < 1초
□ 리렌더링 최소화 (React.memo 적용)
□ 불필요한 상태 업데이트 제거
```

### 6.2 API 응답 시간

```bash
□ /api/health/metrics: < 500ms
□ /api/health/meal-impact: < 800ms
□ 에러율 < 1%
```

### 6.3 데이터 캐싱

```typescript
□ 건강 메트릭스 클라이언트 캐싱 (1시간)
□ 식단 효과 예측 메모이제이션
□ React Query 또는 SWR 통합 (선택)
```

---

## ✅ Phase 7: 사용자 경험 검증

### 7.1 접근성 (Accessibility)

```bash
# WCAG 2.1 AA 기준 준수
□ 키보드 네비게이션 가능
□ 스크린 리더 지원 (ARIA 레이블)
□ 색상 대비 비율 4.5:1 이상
□ 포커스 인디케이터 명확
```

### 7.2 반응형 디자인

```bash
# 다양한 화면 크기 테스트
□ 모바일 (320px - 480px)
□ 태블릿 (768px - 1024px)
□ 데스크톱 (1280px+)

# 터치 인터랙션
□ 버튼 크기 최소 44x44px
□ 스와이프 제스처 지원
```

### 7.3 로딩 및 에러 상태

```bash
□ 스켈레톤 UI 표시
□ 에러 메시지 명확
□ 재시도 버튼 제공
□ 오프라인 상태 처리
```

---

## ✅ Phase 8: 테스트 시나리오

### 8.1 단위 테스트 (Jest + React Testing Library)

```typescript
// 테스트 파일 생성 여부 확인
□ components/health/visualization/__tests__/HealthDashboard.test.tsx
□ components/health/visualization/__tests__/HealthMetricsCard.test.tsx
□ app/api/health/metrics/__tests__/route.test.ts

// 테스트 커버리지 목표
□ 컴포넌트 렌더링: 80%+
□ API 로직: 90%+
□ 계산 함수: 95%+
```

### 8.2 통합 테스트 (Playwright)

```typescript
// E2E 테스트 시나리오
□ 사용자 로그인 → 건강 대시보드 접근
□ 식단 카드 클릭 → 상세 페이지 이동
□ 건강 메트릭스 표시 확인
□ 식단 효과 예측 표시 확인
```

### 8.3 수동 테스트 시나리오

```markdown
시나리오 1: 첫 방문 사용자
1. 건강 정보 미입력 상태
2. 건강 대시보드 접근
3. "건강 정보를 입력해주세요" 메시지 표시 확인
4. 건강 정보 입력 페이지로 이동 유도

시나리오 2: 아침 식단 확인
1. 오늘 날짜의 아침 식단 카드 클릭
2. 아침 상세 페이지 이동
3. 현재 건강 상태 시각화 확인
4. 아침 식사 효과 예측 확인
5. 건강 인사이트 메시지 확인

시나리오 3: 하루 식단 완료 후
1. 저녁 식단 상세 페이지 접근
2. 하루 누적 건강 효과 확인
3. 일일 칼로리 목표 달성도 확인
4. 내일 추천 식단 미리보기 (선택)
```

---

## ✅ Phase 9: 보안 및 개인정보 보호

### 9.1 데이터 보안

```bash
□ 건강 데이터 암호화 (전송 시 HTTPS)
□ 사용자별 데이터 격리 (RLS 정책)
□ API 인증 토큰 검증
□ SQL 인젝션 방지
```

### 9.2 개인정보 처리

```bash
□ 개인정보 처리방침 업데이트
□ 건강 데이터 수집 동의 확인
□ 데이터 삭제 요청 처리 가능
□ GDPR/CCPA 준수 (필요시)
```

---

## ✅ Phase 10: 배포 전 최종 점검

### 10.1 프로덕션 빌드 테스트

```bash
# 빌드 명령어
pnpm build

□ 빌드 성공 (오류 0개)
□ 번들 크기 확인 (< 500KB per chunk)
□ Tree shaking 확인
□ 코드 스플리팅 확인
```

### 10.2 환경 변수 설정

```bash
# .env.production 확인
□ NEXT_PUBLIC_SUPABASE_URL
□ NEXT_PUBLIC_SUPABASE_ANON_KEY
□ SUPABASE_SERVICE_ROLE_KEY
□ NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY
□ CLERK_SECRET_KEY
```

### 10.3 모니터링 및 로깅

```bash
□ Sentry 또는 LogRocket 통합 (선택)
□ API 응답 시간 모니터링
□ 에러율 추적
□ 사용자 행동 분석 (선택)
```

---

## 📊 통합 완료 체크리스트 요약

### 필수 항목 (Must Have)
- [ ] ✅ 타입 정의 완료 및 검증
- [ ] ✅ 모든 컴포넌트 정상 렌더링
- [ ] ✅ API 엔드포인트 정상 작동
- [ ] ✅ 인증 및 권한 확인
- [ ] ✅ 기본 에러 처리 구현
- [ ] ✅ 모바일 반응형 디자인

### 권장 항목 (Should Have)
- [ ] 🔶 단위 테스트 작성 (80%+ 커버리지)
- [ ] 🔶 성능 최적화 (캐싱, 메모이제이션)
- [ ] 🔶 접근성 개선 (WCAG AA 준수)
- [ ] 🔶 사용자 피드백 수집 메커니즘

### 선택 항목 (Nice to Have)
- [ ] 🟢 E2E 테스트 작성
- [ ] 🟢 고급 차트 라이브러리 통합 (Recharts)
- [ ] 🟢 실시간 건강 데이터 동기화
- [ ] 🟢 웨어러블 기기 연동

---

## 🚨 알려진 이슈 및 제한사항

### 현재 제한사항
1. **건강 메트릭스 계산**: 간단한 추정 공식 사용 (실제 의료 기기 데이터 아님)
2. **질병 위험도**: 기본적인 BMI 및 연령 기반 추정 (전문 의료 진단 아님)
3. **비타민/미네랄 레벨**: 식단 기반 추정치 (실제 혈액 검사 결과 아님)

### 권장 사항
- 사용자에게 "이 정보는 참고용이며 의료 진단을 대체하지 않습니다" 경고 메시지 표시
- 심각한 건강 문제 발견 시 전문의 상담 권장 링크 제공

---

## 📞 문제 해결 가이드

### 자주 발생하는 오류

#### 1. 타입 오류: `Property 'X' does not exist`
```bash
해결: types/health-visualization.ts에 해당 속성 추가
또는 타입 정의 import 경로 확인
```

#### 2. 컴포넌트 렌더링 실패
```bash
해결: 
1. 필수 props 전달 확인
2. 데이터 로딩 상태 확인 (null/undefined 처리)
3. console.log로 데이터 구조 확인
```

#### 3. API 401 Unauthorized
```bash
해결:
1. Clerk 인증 토큰 확인
2. Supabase RLS 정책 확인
3. 환경 변수 설정 확인
```

#### 4. 빌드 실패
```bash
해결:
1. pnpm clean (node_modules 삭제 후 재설치)
2. pnpm tsc --noEmit (타입 오류 확인)
3. .next 폴더 삭제 후 재빌드
```

---

*이 체크리스트를 단계별로 진행하면 건강 시각화 기능을 안전하게 프로젝트에 통합할 수 있습니다.*
