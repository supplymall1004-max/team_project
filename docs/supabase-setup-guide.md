# 🔗 Supabase 연결 가이드 (초보자용)

맛의 아카이브 프로젝트에 Supabase를 연결하는 방법을 단계별로 안내합니다.

---

## 📋 전체 과정 요약

1. ✅ Supabase 계정 만들기
2. ✅ 새 프로젝트 생성
3. ✅ API 키 확인하기
4. ✅ 환경 변수 파일 만들기
5. ✅ 연결 확인하기

---

## 1단계: Supabase 계정 만들기

### 1.1. Supabase 웹사이트 접속
- 브라우저에서 [https://supabase.com](https://supabase.com) 접속
- 오른쪽 위 **"Start your project"** 또는 **"Sign in"** 버튼 클릭

### 1.2. 회원가입
- **GitHub 계정으로 가입** (추천) 또는 이메일로 가입
- GitHub로 가입하면 더 빠르고 간편합니다

---

## 2단계: 새 프로젝트 생성

### 2.1. 프로젝트 생성 시작
- Supabase 대시보드에서 **"New Project"** 버튼 클릭

### 2.2. 프로젝트 정보 입력

다음 정보를 입력하세요:

| 항목 | 설명 | 예시 |
|------|------|------|
| **Organization** | 조직 선택 (처음이면 새로 만들기) | My Organization |
| **Project Name** | 프로젝트 이름 | `flavor-archive` |
| **Database Password** | 데이터베이스 비밀번호 | ⚠️ **반드시 기록해두세요!** |
| **Region** | 서버 위치 | **Northeast Asia (Seoul)** 추천 |

### 2.3. 프로젝트 생성 대기
- **"Create new project"** 버튼 클릭
- 약 2-3분 정도 대기 (프로젝트가 생성되는 중입니다)
- 완료되면 대시보드로 이동합니다

---

## 3단계: API 키 확인하기

### 3.1. Settings 메뉴로 이동
- 왼쪽 사이드바에서 **⚙️ Settings** (설정) 아이콘 클릭
- 또는 상단 메뉴에서 **Settings** 클릭

### 3.2. API 메뉴 선택
- Settings 메뉴에서 **"API"** 클릭

### 3.3. 필요한 정보 복사하기

다음 3가지 정보를 복사하세요:

#### 1️⃣ Project URL
- **위치**: "Project URL" 섹션
- **형식**: `https://xxxxxxxxxxxxx.supabase.co`
- **예시**: `https://abcdefghijklmnop.supabase.co`
- ✅ 전체 URL을 복사하세요

#### 2️⃣ anon public key
- **위치**: "Project API keys" 섹션의 **"anon" "public"** 키
- **형식**: `eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...` (매우 긴 문자열)
- ✅ 전체 키를 복사하세요 (눈 아이콘을 클릭하면 보입니다)

#### 3️⃣ service_role key
- **위치**: "Project API keys" 섹션의 **"service_role" "secret"** 키
- **형식**: `eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...` (매우 긴 문자열)
- ⚠️ **이 키는 절대 공개하지 마세요!** 서버에서만 사용합니다
- ✅ 전체 키를 복사하세요 (눈 아이콘을 클릭하면 보입니다)

---

## 4단계: 환경 변수 파일 만들기

### 4.1. .env.local 파일 생성

프로젝트 루트 폴더(`E:\team\flavor_archive`)에 `.env.local` 파일을 만듭니다.

**방법 1: 파일 탐색기에서**
1. `E:\team\flavor_archive` 폴더 열기
2. 새 텍스트 파일 만들기
3. 파일 이름을 `.env.local`로 변경 (앞에 점 포함!)

**방법 2: VS Code에서**
1. VS Code에서 프로젝트 열기
2. 왼쪽 파일 탐색기에서 루트 폴더 우클릭
3. "New File" 선택
4. 파일 이름: `.env.local` 입력

### 4.2. 환경 변수 입력

`.env.local` 파일에 다음 내용을 입력하세요:

```env
# ============================================
# Supabase 설정 (필수)
# ============================================

# 3단계에서 복사한 Project URL을 여기에 붙여넣기
NEXT_PUBLIC_SUPABASE_URL=https://xxxxxxxxxxxxx.supabase.co

# 3단계에서 복사한 anon public key를 여기에 붙여넣기
NEXT_PUBLIC_SUPABASE_ANON_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...

# 3단계에서 복사한 service_role key를 여기에 붙여넣기
SUPABASE_SERVICE_ROLE_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...

# ============================================
# 기본 설정
# ============================================

# 개발 환경 URL
NEXT_PUBLIC_APP_URL=http://localhost:3000
```

**⚠️ 중요:**
- `your_supabase_project_url_here` 부분을 실제 URL로 교체
- `your_supabase_anon_key_here` 부분을 실제 anon key로 교체
- `your_supabase_service_role_key_here` 부분을 실제 service_role key로 교체
- 따옴표(`"`) 없이 입력하세요
- 각 줄 끝에 공백이 없어야 합니다

---

## 5단계: 연결 확인하기

### 5.1. 개발 서버 재시작

환경 변수 파일을 만들거나 수정한 후에는 **반드시 개발 서버를 재시작**해야 합니다.

**터미널에서:**
```bash
# 현재 실행 중인 서버가 있다면 Ctrl+C로 중지
# 그 다음 다시 시작
pnpm dev
```

### 5.2. 연결 확인 방법

#### 방법 1: 브라우저에서 확인
1. 개발 서버 실행 후 `http://localhost:3000` 접속
2. 헤더의 **"로그인"** 버튼 클릭
3. 로그인 모달이 정상적으로 열리면 ✅ 연결 성공!

#### 방법 2: 터미널 로그 확인
- 개발 서버를 실행하면 터미널에 에러가 없으면 ✅ 연결 성공!
- "Supabase 환경 변수가 설정되지 않았습니다" 같은 경고가 없어야 합니다

#### 방법 3: 브라우저 콘솔 확인
1. 브라우저에서 `F12` 키를 눌러 개발자 도구 열기
2. **Console** 탭 선택
3. 빨간색 에러가 없으면 ✅ 연결 성공!

---

## 🎉 완료!

이제 Supabase 연결이 완료되었습니다!

### 다음 단계
- 로그인/회원가입 기능 테스트
- 데이터베이스 테이블 생성 (다음 단계에서 진행)

---

## ❓ 문제 해결

### 문제 1: "Supabase 환경 변수가 설정되지 않았습니다" 에러

**해결 방법:**
1. `.env.local` 파일이 프로젝트 루트에 있는지 확인
2. 파일 이름이 정확히 `.env.local`인지 확인 (앞에 점 포함!)
3. 환경 변수 값이 올바르게 입력되었는지 확인
4. 개발 서버를 재시작했는지 확인

### 문제 2: 로그인 모달이 열리지 않음

**해결 방법:**
1. 브라우저 콘솔(F12)에서 에러 확인
2. Supabase URL과 키가 올바른지 확인
3. Supabase 프로젝트가 활성화되어 있는지 확인

### 문제 3: API 키를 찾을 수 없음

**해결 방법:**
1. Supabase 대시보드 > Settings > API로 이동
2. "Project API keys" 섹션 확인
3. 키 옆의 눈 아이콘(👁️)을 클릭하면 키가 보입니다

---

## 📚 추가 자료

- [Supabase 공식 문서](https://supabase.com/docs)
- [환경 변수 설정 가이드](./env-setup-guide.md)
- [개발 계획서](./development-plan.md)

---

**작성일**: 2025년 1월  
**버전**: V1.0


