# Unity WebGL 빌드 파일이 여기에 배치됩니다

## 빌드 후 파일 구조
```
unity-build/
├── Build/
│   ├── CharacterGame.loader.js
│   ├── CharacterGame.data
│   ├── CharacterGame.framework.js
│   └── CharacterGame.wasm
└── StreamingAssets/ (선택사항)
```

## 빌드 방법
1. Unity 프로젝트에서 WebGL 빌드 실행
2. 빌드 출력 폴더를 이 디렉토리로 설정
3. 빌드 완료 후 파일들이 자동으로 배치됨

